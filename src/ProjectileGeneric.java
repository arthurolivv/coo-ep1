public class ProjectileGeneric extends GameObject {

    public ProjectileGeneric(int state, double x, double y, double vx, double vy, double radius) {
        super(state, x, y, vx, vy, radius);
    }
}
